spring:
  data:
    elasticsearch:
      uris: http://localhost:9200
      username: 
      password:
    redis:
      host: localhost
      port: 6379
      timeout: 2000ms 

elasticsearch:
  host: localhost
  port: 9200
  scheme: http

logging:
  level:
    org.elasticsearch: INFO
    org:
      elasticsearch:
        client: DEBUG
      apache:
        http: DEBUG
    me.yunhui: DEBUG

